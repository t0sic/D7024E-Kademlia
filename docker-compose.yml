services:
  bootstrap:
      build: .
      command: ["--addr=:6881", "--bootstrap", "--id-seed=bootstrap-1"]
  node:
    build: .
    command: ["--addr=:6881", "--peers=bootstrap:6881"]
    depends_on: [bootstrap]
    deploy:
      replicas: 1  # default; override with --scale