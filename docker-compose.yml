services:
  bootstrap:
      build: .
      command: ["--addr=:6882", "--bootstrap", "--id-seed=bootstrap-1"]
  node:
    build: .
    command: ["--addr=:6882", "--peers=bootstrap:6882"]
    depends_on: [bootstrap]
    deploy:
      replicas: 1  # default; override with --scale